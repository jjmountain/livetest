<p id="notice"><%= notice %></p>
<div class="text-xl">
  <%= @test.name %>
</div>
<button class="btn btn-blue float-right" data-reflex="click->QuizReflex#generate_qr">Generate QR Code</button>
<div class="mt-4">
 Questions
</div>
<div id="questions" class="mt-6 bg-blue-100">
 <%= render partial: 'tests/question', collection: @questions %>
</div>

<button class="modal-open bg-transparent border border-gray-500 hover:border-indigo-500 text-gray-500 hover:text-indigo-500 font-bold py-2 px-4 rounded-full">Open Modal</button>
  
 <%= render partial: 'shared/modal', locals: { qr_code: @code }  %>


<%= javascript_pack_tag 'modal', 'data-turbolinks-track': 'reload' %>
